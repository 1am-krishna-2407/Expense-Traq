# Base URL of the API. Keep "/api" in development (Vite proxies it to the backend).
# In production set it to the deployed backend, e.g. https://rupeeflow-api.onrender.com/api
VITE_API_URL=/api

# Dev-server only: where the Vite proxy forwards /api requests.
API_PROXY_TARGET=http://localhost:4000
