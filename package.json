{
  "name": "rupeeflow",
  "private": true,
  "description": "RupeeFlow — Expense Tracker & Budget Management System (monorepo root: convenience scripts only)",
  "scripts": {
    "db:up": "docker compose up -d",
    "db:down": "docker compose down",
    "setup": "npm --prefix backend ci && npm --prefix frontend ci && npm --prefix backend run prisma:generate && npm --prefix backend run prisma:deploy",
    "seed": "npm --prefix backend run db:seed",
    "dev:api": "npm --prefix backend run dev",
    "dev:web": "npm --prefix frontend run dev",
    "test": "npm --prefix backend test && npm --prefix frontend test",
    "test:e2e": "npm --prefix frontend run e2e",
    "typecheck": "npm --prefix backend run typecheck && npm --prefix frontend run typecheck",
    "lint": "npm --prefix backend run lint && npm --prefix frontend run lint",
    "build": "npm --prefix backend run build && npm --prefix frontend run build"
  }
}
